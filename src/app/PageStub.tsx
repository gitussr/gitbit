/**
 * Temporary placeholder used only while routes are scaffolded (Phase 2).
 * Every usage is replaced by real feature UI built on the Design System (Phase 3+).
 */
export function PageStub({ title, description }: { title: string; description: string }) {
  return (
    <section>
      <h1>{title}</h1>
      <p>{description}</p>
    </section>
  )
}
